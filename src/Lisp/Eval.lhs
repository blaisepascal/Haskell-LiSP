> module Lisp.Eval (eval)
>        where

> import Lisp.LispVal

There will be a number of evaluators here, which will be built in separate files

> import Lisp.Eval48 -- Write Yourself A Scheme in 48 Hours

